export interface Store {
  links: any;
  id: string;
  name: string;
  latitude: string;
  longitude: string;
  address: string;
  itemCount: number;
  scrapedItemCount: number;
  lastUpdated: string;
  active: boolean;
}

